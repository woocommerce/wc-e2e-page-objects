/**
 * External dependencies
 */
import { Component } from 'wp-e2e-page-objects';

export default class ComponentOrderShippingAddress extends Component {
	constructor( driver, selector ) {
		super( driver, selector );
	}
}
